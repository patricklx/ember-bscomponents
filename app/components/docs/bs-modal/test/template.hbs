{{#bs-modal onClose=(action 'onClose') as |section|}}
  {{#section.header}}
    the header
  {{/section.header}}
  {{#section.body}}
    the body
  {{/section.body}}
  {{#section.footer}}
    the footer
    {{#bs-button onClick=(action 'doSomething' section.onClose target=modalTarget)}}Close{{/bs-button}}
  {{/section.footer}}
{{/bs-modal}}
