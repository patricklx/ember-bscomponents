{{#if currentModal}}
  {{#if isComponent}}
    {{component currentModal modalTarget=modalManager.modalTarget content=modalManager.properties}}
  {{else}}
    {{log "views/controllers not supported anymore"}}
  {{/if}}
{{/if}}


