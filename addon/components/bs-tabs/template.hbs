{{import Tabs from './tabs'}}
{{import Pane from './pane'}}

{{yield (hash
  tabs=(component Tabs
          selected=@selected
          content=@content
          onItemSelected=@onItemSelected
          contentPath=@contentPath
  )
  pane=(component Pane tagName='')
)}}
