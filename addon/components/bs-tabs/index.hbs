{{import Tabs from './tabs'}}
{{import Pane from './pane'}}

{{yield (hash
  Tabs=(component Tabs
          manager=this
          selected=this.selected
          content=@content
          onItemSelected=this.onItemSelected
          contentPath=@contentPath
  )
  Pane=(component Pane manager=this tagName='')
)}}
