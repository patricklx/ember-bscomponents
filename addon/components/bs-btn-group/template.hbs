{{import isIn from 'ember-bscomponents/helpers/is-in'}}

{{#each content as |item|}}
  {{yield (hash
    item-component=(component itemComponent onClick=(action "onItemSelected" item) isActive=(isIn item selected))
    content=item
  )}}
{{/each}}
