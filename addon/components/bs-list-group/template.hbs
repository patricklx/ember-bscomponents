{{#each content as |item|}}
  {{yield (hash
    item-component=(component itemComponent withLinks=withLinks isActive=(is-equal item selected) onItemSelected=(action 'onItemSelected' item))
    content=item
  )}}
{{/each}}
